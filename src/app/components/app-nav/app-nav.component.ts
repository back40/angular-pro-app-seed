import { Component } from '@angular/core';

@Component({
  selector: 'app-nav',
  styleUrls: ['app-nav.component.scss'],
  template: `
    <div>

    </div>
  `
})
export class AppNavComponent {
  constructor() {}
}
